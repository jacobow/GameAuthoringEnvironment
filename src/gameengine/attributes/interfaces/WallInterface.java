package gameengine.attributes.interfaces;

public interface WallInterface extends AttributeInterface {

	/**
	 * Empty label class that allows Solid
	 * entities to be handled as objects
	 * that can't be passed through
	 */

}
